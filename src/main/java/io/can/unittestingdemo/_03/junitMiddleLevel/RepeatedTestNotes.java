package io.can.unittestingdemo._03.junitMiddleLevel;

public class RepeatedTestNotes {

    // Bir test metodunun birden fazla arka arkaya calismasini istiyorsak repeated test'i kullanabiliriz
    // @RepeatedTest(int n) -> n kac kez calistirilacagi

    // prefix
    // {displayName}, {currentRepetition}, {totalRepetitions} -> name bilgisini bunlar ile duzenleyebiliriz.

    // RepetitionInfo parameter resolver'ini parametre olarak inject edebiliriz.
}
